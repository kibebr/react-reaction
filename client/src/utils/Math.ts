export const average = (arr: number[]): number => arr.reduce((a, b) => a + b) / arr.length
export const secondsToMilliseconds = (x: number): number => x * 1000
export const millisecondsToSeconds = (x: number): number => x / 1000
